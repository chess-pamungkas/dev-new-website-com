# Oqtima website

This project if for Oqtima website frontend

## 1. Local env development

After pulling project from GitHub create `.env.development` configuration file.
To do this copy `.env.example` file, the most of default values should be good.

Default Gatsby domain `http://localhost:8000`

## 2. Deployment