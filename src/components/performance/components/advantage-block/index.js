import React from "react";
import cn from "classnames";

const AdvantageBlock = ({ className, icon, text }) => {
  return <div className={cn("advantage-block", className)}>

  </div>;
};

export default AdvantageBlock;
