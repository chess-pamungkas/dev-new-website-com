import React from "react";
import cn from "classnames";

const Vacancy = ({ className, title, location, area, description }) => {
  return <div className={cn("vacancy", className)}>

  </div>;
};

export default Vacancy;